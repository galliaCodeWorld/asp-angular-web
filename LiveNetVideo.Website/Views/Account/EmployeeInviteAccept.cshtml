@using LiveNetVideo.Website.Services;
@model LiveNetVideo.Website.Models.EmployeeInviteAcceptViewModel

<div class="container">
	<div class="row">
		<div class="col-md-12">

			@if (ViewBag.ErrorMessages != null)
			{
				if (ViewBag.ErrorMessages.Count > 0)
				{

					foreach (string errorMessage in ViewBag.ErrorMessages)
					{
						UtilityHelper.DisplayAlert(errorMessage);
					}
				}
			}

			<div class="jumbotron">
				<h2>Employee Invite Accept</h2>
			</div>
		</div>
	</div>
</div>